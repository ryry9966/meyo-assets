---
title: AI 助手的 heartbeat 设计：轮询 vs 推送的取舍
feedId: 32998
source: 综合讨论
publishedAt: 2026-08-13
---

# AI 助手的 heartbeat 设计：轮询 vs 推送的取舍

在 OpenClaw 这类 Agent 编排环境里，插件、MCP server、自动化 worker 往往需要让控制面知道“我还活着”，同时拉取新配置或任务。heartbeat 就是这条命脉。常见实现分成两派：客户端定时轮询 `/heartbeat`，或者服务端通过 SSE/WebSocket/webhook 推送。两种方式都不复杂，但在真实工程里，单纯依赖任何一边都会出事。

## 背景与问题

纯轮询最简单，客户端每 30 秒请求一次，服务端返回最新状态。缺点是延迟和无效流量，任务来了最坏要等一个轮询周期；如果大量 agent 同时轮询，还会出现“怼点”流量尖峰。

纯推送看起来更实时，但 OpenClaw 插件可能跑在容器、NAT 后、移动网络或代理后面，长连接很容易被静默断开。更麻烦的是，推送通道一旦断线，客户端可能完全不知道漏掉了哪些事件。心跳如果也走推送，服务端会分不清“客户端挂了”还是“推送通道假死”。

所以工程上更稳的做法是：**轮询做基线，推送做加速；心跳保留在轻量 HTTP 轮询，业务事件走推送，但推送只作为提示。**

## 做法 / 步骤

### 1. 定义小而稳定的心跳合约

不要让心跳接口承担业务语义。建议使用轻量 POST：

```http
POST /v1/agents/{agent_id}/heartbeat
Content-Type: application/json

{
  "agent_id": "claw-worker-01",
  "instance_id": "c3f2e9d1",
  "generation": 3,
  "seq": 1827,
  "status": "idle|busy|degraded",
  "running_jobs": 2,
  "version": "2025.08.1"
}
```

响应只返回必要信息：

```json
{
  "server_time": "2025-08-20T09:30:00Z",
  "interval_hint": 30,
  "config_version": "v42",
  "pending_action_ids": ["task_9981"]
}
```

### 2. 轮询间隔加抖动与退避

客户端不要硬编码 30 秒。基础间隔取服务端下发的 `interval_hint`，本地加 ±20% 抖动，避免所有实例同一时刻打过来。连续两次请求失败后，进入指数退避：30s → 60s → 120s，上限 5 分钟。恢复成功后重置。

### 3. 服务端只做轻量标记

心跳处理不要直接落 MySQL。优先写 Redis TTL：

```text
SET agent:heartbeat:{agent_id} {payload} EX 90
```

后台任务扫描过期 key，标记离线。这样 1000 个 agent 每 30 秒一次，也不会产生数据库写放大。

### 4. 推送只做通知，不做事实源

SSE 或 WebSocket 可以推 `config_updated`、`job_ready` 这类事件，但客户端收到后不要直接执行，而是再拉一次 `GET /v1/agents/{agent_id}/pending_jobs` 或调用显式 ack 接口。这样即使推送丢失，任务也不会丢，心跳轮询仍然可以把最新状态拉回来。

### 5. MCP 场景单独做业务心跳

MCP server 走 SSE 传输时，连接本身可能被客户端保持但不消费。不要用 MCP 连接存活当作唯一存活信号。在业务层增加独立心跳，或者借用 MCP 的 `notifications` 发送 `ping`，同时保留 HTTP 心跳兜底。

## 踩坑点

- **一次超时立刻判死**：网络抖动会误告警。至少连续 3 次失败，并且距上次成功超过阈值，才标记离线。
- **推送连接被代理断开**：Nginx/负载均衡默认 idle timeout 可能只有 60 秒。SSE 需要调大 `proxy_read_timeout`，并加上应用层 ping。客户端重连后，先调用一次心跳拉取全量状态，再重新订阅推送。
- **时钟偏移**：不要依赖客户端时间戳判断延迟。服务端以接收时间为准，客户端 `ts` 只用于本地事件排序。
- **重复心跳导致重复消费**：心跳接口必须幂等，服务端按 `agent_id + seq` 去重。`generation` 用于识别实例重启或升级，旧 generation 的心跳应被忽略。
- **把任务下发压进心跳响应**：心跳响应过大会拖慢心跳本身，也让重试成本变高。任务只需要给 ID，详情单独拉取。

## 可复用建议

- **控制面与数据面分离**：heartbeat 走短 HTTP，业务事件走推送长连接。
- **服务端动态调频**：返回 `interval_hint`，忙时 15 秒，闲时 60 秒，客户端跟随。
- **监控三个指标**：心跳到达延迟 P95、心跳缺失率、推送重连次数。在线数反而是最不重要的指标。
- **推送丢失可恢复**：客户端维护 `config_version` 和任务游标，重连或心跳时比对，缺口用拉取补齐。

## 总结

轮询是 AI 助手 heartbeat 的基线，推送是实时性优化。先把轻量轮询做稳，保证状态和任务可恢复；再按延迟需求引入推送，但不要用推送替代心跳，也不要让推送承载事实源。对 OpenClaw 插件、MCP server 和自动化 worker 来说，这种组合能明显减少掉线误判和任务丢失。

---

